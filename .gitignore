.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
pgdata/
.langsmith/
eval/results/
.claude/settings.local.json
.moai/

# 로컬 전용 compose 오버라이드 — docker compose가 옵트인 없이 읽는 파일이라
# 커밋되면 전원의 DATABASE_URL이 바뀐다 (#73 리뷰에서 실제로 한 번 걸렸다).
docker-compose.override.yml

# 비공개 메모 — 리포 협력자에 풀스택 팀이 포함돼 있어 docs/internal도 전원이 볼 수 있다.
# 팀 내부 판단·평가·미정 사안은 여기(커밋 안 됨)나 저장소 밖에 둔다.
docs/_private/