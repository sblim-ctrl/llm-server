# 백엔드 팀 회신 — 확인·요청 사항 (초안)

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-08-10 |
| 작성 | LLM팀 (sblim) |
| 수신 | 풀스택(백엔드)팀 |
| 목적 | 2026-08-10 수신 회신(배포 위치·역방향 토큰·내부 API 오픈 현황) 에 대한 확인·요청 사항 정리 |
| 상태 | **초안 — 코드 변경 없음, 실제 발송은 사용자가 직접 진행** |
| 원천 | `docs/claude_session_handoff.md` §3(백엔드 회신 요지 사실 확정표) · §4 Track 1 T4·T6 |

---

안녕하세요, LLM팀입니다. 회신 잘 받았습니다. 아래 6가지를 확인·요청드립니다.

---

## ① 역방향 토큰 — 저희가 생성해서 별도 전달하겠습니다

LLM→백엔드 호출(콜백·내부 API)을 백엔드에서 검증할 토큰 문자열을 저희가 정해 달라고
하신 요청, 확인했습니다. 기존에 드린 값은 반대 방향(백엔드→LLM, 저희가 검증)이라
별개라는 점도 이해했습니다.

→ 새 토큰 값은 저희가 생성해서 **보안 채널로 별도 전달**하겠습니다. 이 문서를 포함해
어떤 문서에도 값 자체는 남기지 않습니다.

---

## ② receiptPath 확인 요청

2026-08-06 합의는 "백엔드가 지출상세 API의 영수증 경로 필드를 심사 요청의
`receiptPath`에 담아 보낸다"였습니다. 이번 회신의 "지출상세 응답의 영수증 경로로
접근"이라는 표현이 혹시 **"저희가 상세 조회 응답에서 직접 읽으라"**는 뜻인지 확인
부탁드립니다.

→ 만약 그렇다면 리스크가 하나 있습니다. 지금 저희 코드(`load_context`)는 지출 상세
응답에서 영수증 경로를 전혀 읽지 않습니다 — 심사 요청의 `receiptPath` 필드만 봅니다.
이 상태로 붙이면 **전 건이 영수증 미첨부로 처리되어 가드레일이 100% 관리자 검토로
넘어갑니다.**

8/6 합의대로 계속 요청 필드에 채워 주시는 것이라면, 저희 쪽 코드 변경은 필요
없습니다. 어느 쪽인지만 알려주세요.

---

## ③ BE-007(멤버명단) — 구현 전까지의 영향 공유

BE-007이 없어도 심사 자체는 막히지 않습니다(저희가 fail-open으로 처리했습니다).
다만 이 API는 **PII 마스킹**과 **판례 저장**에 쓰입니다. 구현 전까지는:

- 팀원 실명이 마스킹 없이 그대로 LLM에 노출됩니다.
- 관리자 결정에 대한 판례가 저장되지 않습니다.

→ 가능하시면 구현을 요청드립니다.

---

## ④ BE-009(지출이력) — 구현 전까지의 영향 공유

BE-009가 구현되기 전까지는 아래 4개 기능이 동작하지 않습니다.

- 대시보드 AI 요약
- 정산 리포트
- 주간 다이제스트
- 예산 배분 제안

**대시보드는 동기 엔드포인트라 프론트에 500을 직접 반환**하므로 영향이 특히 큽니다
(페이지 로드 경로에 걸립니다). 나머지 3개는 잡 큐 경로라 재시도 후 실패 상태로만
남고 화면에 즉시 드러나지는 않습니다.

→ 이 사실을 **프론트 팀에도 함께 알려주시길** 요청드립니다.

---

## ⑤ EIP·검증용 지출 ID — 회신 대기 중

AWS EC2 Elastic IP 배포가 완료되면 고정 IP·base URL·검증용 지출 ID를 전달받기로
한 것, 확인했습니다. 검증용 팀 ID는 `1`로 이미 확정된 것도 확인했습니다. 배포 완료
후 전달 부탁드립니다.

---

## ⑥ 저희 쪽 GCP 고정 IP — 예약 후 전달 예정

저희 쪽 GCP 외부 IP는 아직 예약 전입니다. 예약 완료 후 전달드리겠습니다.

---

> 근거: `docs/claude_session_handoff.md` §3 사실 확정표, §4 Track 1 T4·T6.
