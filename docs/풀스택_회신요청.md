# 회신 요청 — LLM팀 → 풀스택(백엔드)팀

| 항목 | 내용 |
|---|---|
| 최종 갱신 | 2026-08-04 (8/4 회신 3건 반영 — 3번 · 15번 ① · 16번 ⑥) |
| 함께 보는 문서 | `풀스택_연동_계약.md`(기술 계약) · `openapi.json`(엔드포인트 스펙) · `백엔드_요구_내부API_명세.md`(1번의 구체안) · `카테고리_매핑표_초안.html`(3번의 구체안) |

안녕하세요, LLM팀입니다. 공유해 주신 API 명세서와 DB 테이블 스키마 잘 받았습니다.
저희 코드와 하나씩 맞춰본 결과 그동안 막혀 있던 것 5가지가 덕분에 풀렸습니다 — 관리자 검토로
넘어간 건의 AI 소견을 어디 저장할지, `escalationThreshold`가 금액이라는 것, 카테고리별 예산
한도는 없다는 것, 회칙 버전 관리 방식, 승인/반려를 풀스택 팀에서 실행하신다는 것. 저희 코드에 바로
반영했습니다. 감사합니다.

대신 새로 확인이 필요해진 것을 아래에 급한 순서대로 정리했습니다.
**전부 데모 일정에는 영향 없는 항목**이니 편하신 시점에 답 주시면 됩니다.

---

## 🔴 A. 이게 안 되면 실제 연동 자체가 불가능합니다 (1~3)

### 1. (R1) 저희가 호출해야 하는 내부 조회 API가 명세서에 하나도 없습니다

심사를 하려면 저희가 백엔드에 8가지를 되물어야 합니다 — 지출 상세, 예산, 팀 설정, 모임 유형,
멤버 명단, 회칙 원문, 영수증 이미지, 관리자 결정 내역입니다. 받은 명세서(API-001~050)는
화면에서 쓰는 API만 담고 있어서 이 8가지가 하나도 없습니다.

→ **내부용 API 명세를 따로 열어 주세요.** 경로와 응답 필드만 정해 주시면 저희는 파일 한 곳만
바꾸면 됩니다.

> 📎 **저희 제안을 `백엔드_요구_내부API_명세.md`에 정리해 뒀습니다** — 각 API의 경로·파라미터·
> 응답 예시와 우선순위(필수 6 / 권장 1 / 미사용예정 2), 실패 시 동작까지 담았습니다.
> 그대로 쓰셔도 되고 편한 형태로 바꾸셔도 됩니다.

### 2. AI 심사 API(API-045)가 결과를 그 자리에서 돌려주는 형태로 되어 있습니다

지출 건마다 `ai_job_id`·`ai_dispatched_at` 컬럼을 두신 걸 보면 "요청 보내고 나중에 콜백 받는"
구조로 이해했는데, 그렇다면 API-045가 응답하는 순간에는 아직 심사 결과가 없습니다.

→ 접수만 하고 결과는 콜백으로 받는 게 맞을까요? 맞다면 명세서의 응답값 표기만 고치면 됩니다.

### 3. 저희가 추천하는 카테고리 이름이 풀스택 팀 목록과 겹치는 게 하나도 없습니다

지출 카테고리가 7개(회의·IT_인프라·행사·교육·식비·디자인·기타)로 고정되어 있는데, 저희는 모임
유형별로 다른 카테고리를 씁니다(스터디는 `교재/자료비`, 친목은 `숙박/여행비` 같은 식으로 유형당
6개씩). **교집합이 0개**라, 저희가 추천한 값을 그대로 넣으면 저장이 실패합니다.

> ✅ **2026-08-04 회신 — ENUM 확장 대신 매핑으로 확정.** 등록·조회·통계·프론트에 이미 물려 있어
> 확장보다 매핑이 안전하다는 판단, 이해했습니다. 저희는 카탈로그를 그대로 유지하겠습니다.

**다만 주신 매핑표 6종은 저희 5개 모임 유형 중 `동아리/학생회` 하나의 목록입니다.**
저희 추천 카테고리는 유형마다 달라서 총 30종(유형당 6개 × 5유형)이고, 나머지 4유형 24종은
아직 매핑이 없습니다. **저희가 24종 초안을 만들어 정리해 두었습니다** —
📎 [`카테고리_매핑표_초안.html`](카테고리_매핑표_초안.html)

→ **남은 회신은 세 가지입니다** (상세는 위 문서에 있습니다)
① 신규 제안 24종 승인 여부와, 판단이 갈리는 4건(`온라인/구독비`·`실습/프로젝트비`·
`활동/프로그램비`·`공간/대관비` vs `장소/예약비`) 확정
② `홍보/콘텐츠비`→`디자인` 재검토 결과 — **취소하시면 `디자인` ENUM으로 오는 카테고리가 0이 됩니다**
③ 콜백 `suggestedCategory`가 따르는 목록 (16번 ⑥과 같은 건)

> 참고로 **`IT_인프라`는 매핑 대상이 아니라고 하셨는데, 회사 유형의 `업무도구/소프트웨어비`가
> 여기에 해당합니다.** 동아리 6종만 보면 맞는 말씀이었습니다.
>
> 그리고 이 매핑으로 **30종 중 12종(40%)이 `기타`로 모입니다.** 특히 친목은 6종 중 5종이 기타라
> 식비 하나만 남습니다. 카테고리 확장을 다시 요청드리는 것은 아니고, 나중에 카테고리별 통계
> 요구가 올라올 때 참고하시라고 수치만 남깁니다.

---

## 🟡 B. 동작은 하지만 요구사항이 충족되지 않습니다 (4~7)

### 4. (Q1 재확인) 지출을 등록하는 순간 자동으로 승인되는 것처럼 적혀 있습니다

API-016 설명에 "팀 설정에 따라 등록 시점의 status가 SUBMITTED / ESCALATED / 승인됨 중 하나로
자동 결정"이라고 되어 있는데, AI 심사를 거치지 않고 승인이 되면 "AI가 함부로 승인하지 않도록
막는 장치"가 무력화되고 AI 심사 자체가 의미가 없어집니다.

→ 등록 시점에는 항상 `SUBMITTED`이고, 승인은 저희 콜백을 받은 뒤에만 되는 게 맞을까요?
(7/15에도 여쭤봤던 항목입니다)

### 5. (Q4③) 관리자가 `escalationThreshold`를 바꿀 방법이 없습니다

"이 금액 넘으면 무조건 관리자 검토"에 쓰는 값인데, DB에서는 필수값(NOT NULL)인 반면 팀 설정
조회(API-028)·수정(API-029) 어디에도 이 필드가 없습니다. 모임 만들 때(API-009) 응답에만
나옵니다.

**공유해 주신 AI 마법사 2단계가 바로 이 값을 관리자가 직접 정하는 화면이었습니다**("관리자 확인
설정 금액", 10/20/30만원 프리셋). 화면 안내에도 "여기서 설정한 값은 에이전트 정책 화면에서
언제든 바꿀 수 있어요"라고 되어 있는데, 조회·수정 API에 필드가 없으면 그 화면이 동작하지 않습니다.
같은 화면의 "모든 지출 직접 확인할래요" 토글(= `autoApprove`)도 마찬가지입니다.

→ 조회·수정 API에 `escalationThreshold`·`autoApprove` 두 필드를 추가해 주시고, 모임 생성 시
설정을 생략하면 어떤 기본값이 들어가는지도 알려주세요.

### 6. (R5) 알림에 AI가 판단한 사유를 담을 수가 없습니다

알림 문구를 조회 시점에 `expenses`·`users`만 조인해서 만드신다고 되어 있는데, AI 소견은
`audit_logs`에 저장되기 때문에 "왜 관리자 검토로 넘어갔는지"를 알림에 넣을 수가 없습니다.

→ 알림 문구를 만드실 때 `audit_logs`도 함께 조인해 주실 수 있을까요?

### 7. 회칙이 바뀐 걸 저희가 알 방법이 없습니다

회칙을 등록(API-031)하거나 수정(API-033)하셨을 때 저희에게 알려주는 호출이 명세에 없습니다.
저희는 회칙을 미리 읽어두고 심사하기 때문에, 알림을 못 받으면 계속 옛날 회칙으로 심사하게 됩니다.

**AI 마법사 3단계가 회칙을 등록하는 화면(파일 업로드·직접 입력)이라 이 항목의 급한 정도가
올라갔습니다.** 지금 상태로는 관리자가 마법사에서 회칙을 등록하고 "설정 완료"까지 눌러도,
그 회칙이 심사에 전혀 반영되지 않습니다.

→ 회칙이 바뀔 때 저희 쪽에 `{teamId, 변경유형, 버전}` 세 가지만 보내주시면 됩니다
(`POST /v1/context/refresh`). 원문은 저희가 다시 조회하겠습니다.

덧붙여 API-046(심사 결과 조회)에 "관리자 검토 대기 중에는 결과가 비어서 404를 줄지 null을 줄지
확인 필요"라고 적어두셨는데, 그 경우 소견은 API-022(처리 이력)에서 보이므로 **API-046은 null로
고정**하시는 편이 프론트에서 단순할 것 같습니다.

---

## 🟡 C. 운영 규모·개인정보 관련 (8~10) — LLM팀 신규 제기

### 8. 심사 결과를 몇 초 안에 드려야 하는지 확정해 주세요

`bravo 기술아키텍처설계서`에 *"일정 시간(예: 30초) 안에 콜백이 오지 않으면 자동으로 ESCALATED로
전환한다"* 고 되어 있는데 30초가 확정치인지 불명확합니다. **저희 실측치를 먼저 공유드립니다.**

| 구간 | 소요 |
|---|---|
| 큐 대기 (워커 폴링 2초 주기) | 최대 2.0초 |
| 심사 그래프 실행 | 중앙값 **3.3초** / 최대 **4.4초** |
| 콜백 HTTP 왕복 | 수백 ms |
| **단건 합계(추정)** | **약 5~7초** |

→ 단건 기준으로는 30초 예산에 충분히 들어갑니다(최악 케이스도 약 25초 여유).
영수증 금액이 신청과 다른 건은 LLM 호출 없이 0.4초에 조기 종료됩니다.

**다만 동시에 몰리면 초과합니다.** 현재 워커는 1대가 1건씩 순차 처리해서, 큐 8번째 건은
대기 28초 + 심사 4초 = 32초로 넘어갑니다. 워커 대수를 늘리면 비례해 해소되는 구조입니다.

→ **회신 부탁드립니다**
1. 확정된 제한 시간은 몇 초입니까?
2. **동시 심사 요청이 최대 몇 건까지 예상됩니까?** (예: 동시 20건 → 워커 3대로 맞추겠습니다)
3. 제한 시간을 넘겨 도착한 콜백은 폐기됩니까, 상태 전이 없이 **소견만 저장**됩니까?

> ※ 위 실측에 영수증 이미지 OCR은 미포함입니다(텍스트 변환 기준). 실이미지 심사는
> Vision 호출이 추가돼 2~5초 더 걸릴 것으로 봅니다.

### 9. 콜백 판정 필드의 **이름과 값 형식**을 확정해 주세요

- **현재 저희가 보내는 것**: `verdict` 키에 `approve` / `reject` / `escalate` (**소문자**)
- **명세서 API-045/046 응답 필드**: `finalVerdict`
- 명세서의 상태값은 `SUBMITTED`/`ESCALATED`/`APPROVED`/`REJECTED`(**대문자**)인데,
  **`finalVerdict`의 값 형식은 명시되어 있지 않습니다.**

→ ① 콜백 키를 `finalVerdict`로 바꿀까요? (풀스택 팀에서 `verdict`로 매핑 중이면 현행 유지)
② 값은 대문자입니까 소문자입니까? ③ 저희 `escalate`는 status `ESCALATED`와 1:1 대응입니까?

### 10. `detail` 한 필드를 어떻게 만들지 정해 주세요 ⚠️ 개인정보

API-045/046 응답의 `review.detail`은 **단일 문자열**인데, 저희는 재료를 **3가지로 나눠서**
보냅니다.

| 저희가 보내는 것 | 성격 |
|---|---|
| `reasons.requester` | **요청자용** — 정중하고, **금액·한도 등 수치를 넣지 않음** |
| `reasons.admin` | **관리자용** — 회칙 조항·수치를 인용 |
| `opinions[]` | 심사관 3종(회칙·예산·판례) 소견 원본 |

→ ① `detail`은 백엔드에서 위 재료로 조합하십니까, 저희가 완성된 문자열로 보내야 합니까?
② **`detail`이 요청자(지출 신청자)에게도 보이는 화면이 있습니까?** — 있다면 `reasons.admin`의
수치가 신청자에게 노출되지 않도록 저희가 필드를 분리해 보내야 합니다. **개인정보·정책 문제라
꼭 확인 부탁드립니다.**

---

## ⚪ D. 기능 영향은 작고, 서로 안 맞는 부분을 맞추는 것들 (11~17)

### 11. (R6) 회칙 추천 API(API-044)가 저희 쪽과 형식이 안 맞습니다

저희가 내려주는 건 조항 목록 + 추천 승인기준(자동승인 한도 등) + 추천 카테고리인데, 명세의
응답은 `{제목, 등록방식, 내용}` 형태라 추천 승인기준을 담을 자리가 없습니다.

✅ **① `teamId` 문제는 화면 설계를 보고 저희 쪽에서 정리했습니다 — 더 답 안 주셔도 됩니다.**
모임 생성 화면에서 "모임 만들고 설정 시작하기"를 누른 뒤 마법사로 들어가는 흐름이라, 마법사
진입 시점에는 이미 `teamId`가 있습니다(3단계 회칙 등록도 `teamId` 없이는 안 되니 앞뒤가 맞습니다).
저희 `/v1/policy-draft`는 `teamId`를 받지 않으니, **API-044에서 `teamId`로 팀을 조회하신 뒤
`teamType`·`teamName`·`initialBudget`·모임 소개로 바꿔서 저희를 호출**해 주시면 됩니다.
저희가 예전에 드렸던 "teamId 없이 호출" 요청은 필요 없어졌습니다.

→ 남은 두 가지만 확인 부탁드립니다. ② 추천 승인기준은 버리는 건지, 아니면 팀 설정 수정(API-029)으로
따로 넣으시는 건지 ③ 모임 초기 예산 컬럼을 `teams`에서 빼셨는데 저희는 초안을 만들 때 그 금액이
필요합니다 — 예산 테이블에서 꺼내주시면 될까요?

### 12. 대시보드 AI 요약(API-024)을 어떤 방식으로 만들지 정해야 합니다

저희는 이 기능을 다음 스프린트로 미뤄뒀는데 명세에 정식으로 들어가 있어서 다시 잡았습니다.

→ 풀스택 팀에서 집계한 숫자를 넘겨주시고 문구만 받으시는 방식이면, 저희가 그에 맞는 API를 새로
만들겠습니다. 넘겨주실 집계 항목 목록만 알려주세요. 정산 리포트 요약도 같은 방식으로 갈지 함께
정하면 좋겠습니다.

### 13. 명세서에서 세 군데가 어긋나 있습니다

① `audit_logs`의 `event_type` 목록에 값이 비어 있는 자리가 하나 있고, 설명에는 `EDITED`가
언급되는데 목록에는 없습니다. 그리고 "AI 심사가 끝나서 소견을 저장했다"에 해당하는 이벤트가
없어서, 저희 콜백이 도착했을 때 어떤 값으로 기록되는지 알 수 없습니다.
② 모임 초기 예산이 DB(`teams`)에서는 빠졌는데 API-009 요청·응답에는 그대로 남아 있습니다
(11번 ③과 같은 건입니다).
③ API-046(AI 심사 결과 조회) 설명이 "AI 지출 심사(API-044) 결과를 …"로 되어 있는데,
AI 지출 심사는 API-045이고 API-044는 AI 정책 추천입니다. 번호만 잘못 적히신 것 같습니다.

→ `event_type` 목록을 확정해 주세요.

### 14. 저희 API의 필드 표기가 두 군데 섞여 있습니다

① 심사 요청(`POST /v1/analyze`)은 받을 때는 `jobId`처럼 camelCase인데 응답은 `job_id`처럼
snake_case입니다(`GET /v1/jobs/{id}` 응답도 동일). ② 콜백은 최상위가 전부 camelCase인데
`opinions[].similar_cases` 하나만 snake_case로 나갑니다.

→ 저희 판단으로는 **전부 camelCase 통일**이 자연스러운데, 풀스택 팀 파서에 이미 반영하신 게 있으면
그 기준을 알려주세요. 저희는 파일 한두 곳만 고치면 됩니다. 임의로 바꾸면 계약이 또 어긋나서
현상 그대로 전달드립니다.

> 상세 예시는 `풀스택_연동_계약.md` §4에 있습니다.

### 15. 모임 생성·AI 마법사 화면과 저희 API가 맞지 않는 부분

화면 설계 4장(모임 생성 · 마법사 1~3단계) 잘 받았습니다. 저희 `POST /v1/policy-draft`와
하나씩 맞춰본 결과를 정리했습니다. **모임 유형 5종·모임 이름·모임 소개·카테고리 자동 추천은
그대로 맞습니다.** 아래 여섯 가지만 확인 부탁드립니다.

**① 회비 금액을 저희에게도 보내주실 수 있을까요? (1단계)**
화면 설명이 "AI가 예산 심사 시 기준으로 활용해요"인데, 지금까지 저희가 받는 금액은 초기 예산
하나뿐이었습니다. **저희 쪽에 `dues`(1인당 회비) 필드를 이미 열어 뒀으니**(`openapi.json`의
`PolicyDraftRequest` 참조) API-044 요청에 실어 주시기만 하면 됩니다. 안 보내주셔도 지금처럼
동작합니다. 저희는 이 값을 **회칙 초안의 회비 조항**에 씁니다.
> ✅ **2026-08-04 회신 — `0`으로 확정** (`membershipfee`가 NOT NULL DEFAULT 0).
> 저희는 `null`과 `0` 둘 다 '없음'으로 처리하고 있어 **코드 변경 없이 그대로 동작합니다.**
> 이 항목은 종결입니다.

> 참고로 회비를 **자동승인 한도 계산에는 넣지 않았습니다.** 회비는 1인당 금액인데 인원 수를
> 받는 경로가 없어서(②번) 총액으로 환산할 수가 없고, 그 상태로 한도 공식에 넣으면 근거 없는
> 규칙을 저희가 임의로 만드는 셈이 됩니다. 인원 수 문제가 정리되면 반영하겠습니다.

**② 무료 플랜 20명 제한은 풀스택 팀에서 확인하시는 게 맞을까요?**
저희 요청 스키마에 `memberCount`라는 선택 필드가 있긴 한데 상한 검증이 없고, **모임 생성 화면에
인원을 입력하는 칸이 없어서** 이 값을 어디서 받아야 할지 모르겠습니다. 정원 제한은 플랜·과금
쪽이라 백엔드에서 보시는 게 맞을 것 같은데, 맞다면 저희는 이 필드를 그냥 비워두고 쓰겠습니다.

**③ 모임 이름과 초기 예산도 필수 아닌가요?**
모임 생성 화면에는 필수 표시(`*`)가 모임 유형에만 붙어 있는데, 저희 API는 **모임 이름과 초기
예산도 필수이고 초기 예산은 0보다 커야 합니다.** 화면대로 비운 채 넘어오면 저희가 422로 거절하게
됩니다. 화면에 필수 표시를 맞춰 주시거나, 아니면 저희 쪽을 선택값으로 풀어야 하는데 어느 쪽이
좋을지 알려주세요. (11번 ③의 초기 예산 조달 경로와 같은 건입니다)

**④ 고액 기준 20만원 — 저희 쪽은 화면에 맞췄습니다 (2단계)**
저희는 자동승인 한도의 **6배**를 강제 관리자 검토 금액으로 제안하고 있어서, 소액 기준이
5만원이면 고액 기준은 30만원이 나왔습니다. 화면 표(소액 5만 미만 / 중간 5~20만 / 고액 20만 이상)에
맞추려면 6배가 아니라 **4배**여야 해서, **저희 계산을 4배로 바꾸고 코드 기본값도 300,000원 →
200,000원으로 내렸습니다.** 이제 초기예산 100만원짜리 모임이면 화면 표와 정확히 같은
5만/20만이 나옵니다.
→ 풀스택 팀 `escalationThreshold`의 **기본값도 200,000원으로 잡으실지**만 알려주세요.

**④-2 그런데 화면의 5만/20만은 고정 문구인가요, 저희 제안값을 반영하는 칸인가요? (2단계)**
위 계산이 **초기예산 100만원·모임 유형 동아리/스터디/동호회**일 때 정확히 5만/20만이 나온다는
뜻입니다. 저희는 모임 유형별로 자동승인 비율을 다르게 두고 있어서(친목이 가장 후하고 회사가 가장
빡빡합니다), 같은 100만원이라도 **친목이면 6만/24만, 회사면 3만/12만**이 나옵니다. 예산이
100만원이 아니면 또 달라집니다.
→ 화면 표의 "50,000원 미만 / 200,000원 이상"이 **모든 모임에 똑같이 박히는 고정 문구**라면
저희 제안값과 화면이 어긋나 보일 수 있습니다. 반대로 **저희 `/v1/policy-draft` 응답의
`policy_params`를 받아 그려 주시는 칸**이라면 그대로 두시면 됩니다. 어느 쪽인지만 알려주세요.
(참고로 화면에서 관리자가 직접 고르는 것은 고액 기준 하나뿐인데, 저희 제안은 소액·고액을 4배로
묶어서 냅니다. 관리자가 10만원이나 30만원을 고르셨을 때 소액 기준을 같이 움직이실지, 5만원으로
두실지도 함께 정해 주시면 저희가 맞추겠습니다.)

**⑤ 중간 지출의 "대기/자동"은 어떤 뜻인가요? (2단계)**
표의 소액·고액은 저희 금액 기준과 1:1로 맞는데, 중간 구간만 저희 쪽에 대응하는 개념이 없습니다.
저희는 이 구간을 금액이 아니라 **AI 확신도(기본 0.8)** 로 가릅니다 — 확신하면 자동, 아니면 관리자
대기입니다. 화면 문구가 그 뜻이면 저희는 바꿀 게 없습니다.

**⑥ "건너뛰기 → 기본 정책 모드"가 무엇을 뜻하나요? (3단계)**
회칙 없이 심사하게 되는 경우인데, 저희 쪽에 정의가 없습니다. **저희가 갖고 있는 모임 유형별
기본 조항(영수증 필수·1인당 식비 한도 등)을 적용한다**는 뜻으로 이해하면 될까요? 그렇게 정해
주시면 저희가 그 동작을 명시적으로 구현하겠습니다.

> 참고로 화면 안에서 **단계 순서가 서로 다르게 그려져 있습니다** — 1·3단계 상단은
> `회비·예산 → 회칙·규정 → 승인 정책`인데 2단계 상단은 `회비·예산 → 승인 정책 → 회칙·규정`이고,
> 2단계에 "설정 완료", 3단계에 "다음" 버튼이 붙어 있어 어느 쪽이 마지막인지 헷갈립니다.
> 저희 계약에는 영향이 없지만, **회칙 등록이 몇 번째인지에 따라 7번의 알림 시점이 달라져서**
> 확정되면 알려주세요.

### 16. 지출 상세 화면을 보고 콜백에서 뺄 값을 골랐습니다 — 확인 부탁드립니다

Figma 설계도를 직접 열어봤습니다. 그동안 저희가 받은 건 이미지 4장(모임 생성·마법사 1~3단계)
이었는데, **지출 상세 화면 4종이 저희 콜백의 유일한 소비처**더군요. 그걸 못 본 채로 15필드를
보내고 있었습니다. 화면에 맞춰 정리한 결과를 드립니다.

**① 화면에 안 나오는 4필드를 뺐습니다 — 쓰고 계셨다면 되돌리겠습니다** ⚠️ 계약 변경

`modelVersion` · `promptVersion` · `costUsd` · `latencyMs` 네 개는 24화면 어디에도 표시되지
않습니다. 저희가 성능·비용을 보려고 넣었던 값이라 **콜백에서 빼고 저희 잡 기록에만 남기도록
바꿨습니다.** 콜백 페이로드는 **15필드 → 11필드**가 됩니다
(`풀스택_연동_계약.md` §3 갱신 완료).

→ **혹시 이 값들을 저장하거나 쓰고 계셨다면 알려주세요. 바로 되돌리겠습니다** — 저희 쪽
수정은 몇 줄이라 부담이 없습니다. 나머지 11필드는 그대로입니다.

**② `mismatch`와 `confidence`는 일단 그대로 두었습니다**
둘 다 화면에 안 보이지만, 에스컬레이션 화면 문구가 *"불일치 항목을 확인 후 직접…"* 이라
`mismatch`를 가리키고 있어서 지우지 않았습니다. 저장해서 쓰고 계신지만 알려주세요.

**③ `판정 근거 · …` 한 줄은 누가 만드나요?**
화면은 심사관마다 근거가 **한 줄**인데, 저희는 근거를 **세 갈래로 나눠** 보내고 있습니다 —
조항(`evidence`) · 수치(`figures`) · 유사건(`similar_cases`). 화면 예시를 보면
`회칙 제7조 …`(조항)과 `행사비 누적 사용 300,000원, 한도 1,500,000원 (20%)`(수치)이
**같은 자리**에 오더군요.
→ **저희가 한 줄로 합쳐 보내드릴까요, 아니면 지금처럼 셋을 받아 풀스택 팀에서 조합하실까요?**
합쳐 드리는 쪽이면 저희가 필드 하나 추가하겠습니다.

> **10번과 같은 결의 질문입니다.** 10번은 `review.detail`(건 전체 사유) 조립 주체를,
> 이건 심사관별 `판정 근거` 한 줄의 조립 주체를 여쭙는 것입니다. 한 번에 답해주셔도 됩니다.

**④ 심사관 판정에 "실패" 상태가 하나 더 있습니다**
화면 뱃지는 `적합` `부적합` `보류` 셋인데, 저희 값은 넷입니다 — 넷째가 `error`(심사관 호출
자체가 실패)입니다. **가정이 아니라 실제로 나옵니다** — 저희 테스트에서 `회칙 심사 실패`가
그대로 콜백에 실리는 걸 확인했습니다. 이때 화면에 무엇을 띄울지 정해 주세요.
(저희가 `보류`로 낮춰 보내는 방법도 있는데, 그러면 "심사를 했는데 보류"와 "심사가 안 됐다"가
구분되지 않아 권하고 싶지 않습니다)

**⑤ "처리 주체"에 들어갈 값을 알려주세요**
저희는 지금 `"AI"` 한 가지만 보냅니다. 화면에는 `AI 에이전트`와 `미처리` 두 가지가 나오는데,
에스컬레이션 화면에서 `미처리`가 뜨는 걸 보면 단순 번역이 아니라 **상태에 따라 달라지는
값** 같습니다. 저희가 상태에 맞춰 보내드릴지, 풀스택 팀에서 변환하실지 정해 주세요.

**⑥ "자동 분류" 값은 어느 목록을 따르나요?**
지출 요청 화면의 카테고리는 7종(`회의` `IT/인프라` `행사` `교육` `식비` `디자인` `기타`)인데,
상세 화면의 "자동 분류"에는 **`강연비`** 가 떠 있습니다. 그 건의 카테고리는 `교육`이고요.
즉 자동 분류가 카테고리 7종과 **다른 목록**을 쓰는 것으로 보입니다.
→ 저희 `suggestedCategory`가 어느 목록에 맞춰야 하는지 알려주세요.

> ⏳ **2026-08-04 회신에 이 질문의 답이 없었습니다.** "콜백 `suggestedCategory`도 이 표기로
> 주시면 바로 매핑됩니다"를 저희 쪽에서 두 가지로 읽었는데, **어느 쪽도 화면과 맞지 않습니다** —
> 7종 ENUM으로 보내면 화면의 `강연비`를 재현할 수 없고, 카테고리 칸과 자동 분류 칸이 늘 같은
> 값이 되어 자동 분류 칸이 무의미해집니다. 근거 표를
> [`카테고리_매핑표_초안.html`](카테고리_매핑표_초안.html) ③에 정리했으니 함께 봐주세요.
>
> 덧붙여 맞물린 항목이 하나 있습니다. `expenses.category`가 `NOT NULL`이라 저희가 지출 상세를
> 조회하면 카테고리가 항상 채워져 옵니다. 저희는 **카테고리가 비어 있을 때만** 자동 분류를 돌리게
> 되어 있어서, 이대로면 `suggestedCategory`가 받은 값을 그대로 돌려주는 형태가 됩니다.
> 화면대로 두 칸이 서로 다른 값을 보여주는 게 맞다면 **저희가 항상 분류하도록 고치겠습니다** —
> 위 질문과 함께 알려주시면 한 번에 반영하겠습니다.

**⑦ 두 화면의 승인 기준이 서로 다른 배수를 쓰고 있습니다**

| 화면 | 자동승인 한도 | 관리자 검토 기준 | 배수 |
|---|---|---|---|
| AI 마법사 2단계 | 50,000원 | 200,000원 | **4배** |
| 예산 관리 시스템 | 100,000원 | 500,000원 | **5배** |

**금액 자체가 다른 건 문제가 아닙니다** — 15번 ④-2에서 말씀드렸듯 저희 추천 한도는 초기예산과
모임 유형에 따라 달라지니, 화면에 어떤 숫자가 박혀 있든 실제 값은 팀마다 다릅니다.

문제는 **두 금액을 잇는 배수**입니다. 저희는 자동승인 한도가 정해지면 관리자 검토 기준을
**항상 그 4배**로 냅니다(15번 ④에서 6배 → 4배로 내린 그 값이고, 상수 하나뿐입니다).
마법사 2단계는 4배(5만→20만)라 맞는데, 예산 관리 시스템은 5배(10만→50만)입니다.
**배수가 하나라서 두 화면을 동시에 만족시킬 수가 없습니다.**

→ 어느 배수가 맞는지 알려주세요. 아니면 **두 값을 관리자가 각각 따로 정하는 것**이라면
저희 배수 계산을 빼고 두 값을 독립적으로 내겠습니다.
(마법사에서 정한 값이 예산 관리 화면의 초기값으로 들어가는 흐름이라면, 두 화면의 배수가
같아야 할 것 같습니다)

**⑧ "AI 추천 예산 관리"는 살아 있는 기능인가요?**
모임 관리 화면에 AI가 쓴 문단 3개(`카테고리별 분석` · `예산 현황 분석` · `AI 추천`)가 있는데,
이걸 만들던 API는 **팀 결정으로 MVP에서 빠진 상태**입니다(`백엔드_요구_내부API_명세.md` 5번).
저희 쪽 생성 코드는 아직 남아 있어서 되살리는 건 어렵지 않습니다.
→ **화면에서 빼실 건지, 저희가 되살릴 건지** 알려주세요.

### 17. 팀별 AI 에이전트는 별도 프로세스가 아니라 데이터로 구성하는 안을 제안합니다

모임마다 LLM 서버나 워커 프로세스를 새로 띄우지 않고, 모든 팀이 공용 프로세스를 사용하되
설정·정책·예산·판례·인덱스를 `teamId`로 분리하는 방식으로 정리하려 합니다.

이 방식에서는 API-009가 “AI 프로세스 생성”을 담당하지 않습니다. 다음 데이터를 한 트랜잭션으로
만들어 마법사 진입에 필요한 `teamId`와 안전한 초기 상태를 제공하면 됩니다.

- `teams`
- `team_members` — 인증 사용자를 `ADMIN`으로 저장
- `budgets`
- 기본 `team_settings` — `autoApprove=false`, `autoApproveLimit=null`,
  `escalationThreshold=200000`

정책 파일은 API-031에서만 받고, 정책 저장 후 Outbox를 통해 `/v1/context/refresh`를 호출해
인덱싱합니다. API-009에 정책이 없는 상태는 아직 설정 중인 `DRAFT`이고, 사용자가 명시적으로
“건너뛰기”를 누르면 유형별 기본 정책을 버전 1로 저장해 `policyMode=DEFAULT`로 구분하는 안입니다.

준비 상태는 프로세스 상태가 아니라 아래 데이터 상태로 표현하는 것을 제안합니다.

```text
setupStatus: DRAFT | READY
policyMode: DEFAULT | CUSTOM | null
policySyncStatus: NOT_STARTED | PENDING | READY | FAILED
```

`policySyncStatus != READY`이면 자동 승인은 막고 관리자 검토로 전환합니다.

→ 아래 4가지를 우선 확인 부탁드립니다.

1. API-009에서 기본 `team_settings` 행을 항상 만들 수 있는지
2. `adminId`를 요청값이 아닌 인증 사용자로 처리할지
3. 위 준비 상태 3종을 어느 테이블·응답에서 관리할지
4. 정책 저장과 LLM 인덱싱 사이에 Outbox를 사용할지

상세 흐름과 API별 권장 필드는
[`풀스택_팀별_AI에이전트_연동제안.md`](풀스택_팀별_AI에이전트_연동제안.md)에 정리했습니다.

---

## 📌 지난번(7/15) 질문 중 아직 답을 못 받은 것

이번 문서로도 풀리지 않아 그대로 남아 있습니다. 참고만 해주세요.

| 안건 | 남은 것 |
|---|---|
| **Q3** 관리자 결정 주기 조회 | 응답 필드는 저희가 제안드렸고, **어느 테이블에서 오는 값인지 · 몇 초 주기인지 · 인증 방식** 세 가지. 위 1번과 함께 답해주시면 됩니다 |
| **Q6** 영수증 미리보기용 임시 토큰 스펙 | 예전 API-050으로 부르던 건인데 번호가 정산 리포트 조회로 바뀌어 기능으로 다시 여쭙니다 |
| **C3** 심사 요청에 실어 주실 필드 3가지 | ① **결과를 어디로 콜백할지 주소를 같이 보내주실지**(현재 저희는 설정값 고정 사용) ② 시험 실행(`dry_run`) 자리를 남겨둘지 ③ '심사 목표'를 자유 텍스트로 주실지 정해진 값 중 하나로 주실지 |
| **C2** 승인/반려 실행 주체 | 저희가 추천만 드리고 풀스택 팀에서 실행하시는 방향으로 확정된 것으로 이해했습니다. 남은 건 둘 — **콜백 응답으로 최종 반영 결과를 돌려주실 수 있는지**, **콜백 중복 도착 시 중복 처리를 막는 책임이 어느 쪽인지** |
| **R2** 콜백 항목 ↔ 풀스택 팀 DB 필드 매핑표 | 위 13번 ①(`event_type`)과 같이 정하면 한 번에 끝납니다 |

---

**급한 순서**: A(1·2·3 — 연동 자체가 막힘) → B·C(4~10) → D(11~17)

감사합니다!
